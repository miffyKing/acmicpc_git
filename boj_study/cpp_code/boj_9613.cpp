#include <iostream>
#include <vector>
#include <math.h>

using namespace std;

int main()
{
    int tc;
    
    cin>>tc;
    for (int idx = 0; idx < tc; idx++)
    {
        int tmp;
        vector<int> n_list;
        cin>>n;
        for (int i = 0; i < n; i++)
        {
            cin>>tmp;
            n_list.push_back(tmp);
        }

    }
    return 0;
}